# task-management-web-app-225782-225791

This workspace contains the Todo frontend React application.

- Container: `todo_frontend`
- Run locally:
  ```
  cd todo_frontend
  npm install
  npm start
  ```
- Configure via `REACT_APP_*` environment variables. See `todo_frontend/README.md` and `.env.example`.